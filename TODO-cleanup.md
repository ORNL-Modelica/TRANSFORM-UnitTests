# TODO: remove legacy buildingspy regression leftovers

Marked 2026-07-06 during the DSTF migration verification run. This repo now
serves as the DSTF reference-results store (`ReferenceResults/testing.json` +
`ReferenceResults/test_spec.json` + `ReferenceResults/<Backend>/<os>/ref_NNNN.json`).
The items below belong to the previous buildingspy-based regression approach
and are superseded by DSTF (`uv tool install dstf`, then
`dstf --config ReferenceResults/testing.json run`). They are kept only until
we are confident nothing still consumes them — then delete.

## To remove

- `createUnitScripts.py` — generated per-test `.mos` scripts + `runAll_Dymola.mos`.
  DSTF discovers tests directly from the library's `UnitTests` components and
  `test_spec.json`; no generated script files needed.
- `runUnitTests.py` — buildingspy driver (required hand-patching installed
  buildingspy internals per the old README instructions).
- `Scripts/Dymola/` and `Scripts/jModelica/` — 658 generated `.mos` files.
  Regenerable artifacts of the old flow; nothing in DSTF reads them.
- `../TRANSFORM-Library/runAll_Dymola.mos` — generated by `createUnitScripts.py`
  into the library repo. Decide separately whether the "run everything in the
  IDE" convenience is worth keeping; if so, regenerate on demand rather than
  tracking it.

## Before deleting, confirm

- [ ] No CI job or personal workflow still invokes `runUnitTests.py` /
      `createUnitScripts.py` (grep any CI configs and local task runners).
- [ ] The README "Legacy" section (marked the same day) can be dropped with them.
- [ ] The `Future work` wishlist items in the old README are all covered by
      DSTF or tracked in the DSTF repo (`docs/ideas.md`) — they largely map to
      what DSTF already does (stable IDs, backend/os-partitioned refs,
      interactive reports, CI-ready JUnit output).
